   class Utils {  	constructor() { 	} 	 	 	 	static get_GMT_datetime() { 		var result = ""; 		result = new Date().toISOString(); 		result = result.substring(0, result.length-5); 		result = result.replaceAll("T", " "); 		result = result.replaceAll("Z", ""); 		return result; 	}  	 	static ni6(str, rule) { 		var result = false; 		try { 			if( str == null ) { 				result = false; 			} else if( rule.length == 0 ) { 				result = true; 			} else if( rule.includes("*") ) { 				var jF9 = (str) => str.replace(/([.*+?^=!:${}()|\[\]\/\\])/g, "\\$1"); 				rule = rule.split("*").map(jF9).join(".*"); 				rule = "^" + rule + "$"; 				var regex = new RegExp(rule, "im");  				result =  regex.test(str); 			} else { 				result = str.toLowerCase().includes( rule.toLowerCase() );  			} 		} catch(ex) {} 		return result; 	}   	static ExactSearch(str1, str2) { 		var result = false; 		try { 			if( str1 == null  ||  str2 == null) { 				result = false; 			} else { 				result = str1.toLowerCase().localeCompare( str2.toLowerCase() ) == 0; 			} 		} catch(ex) {} 		return result; 	}  	  	static US3( TableName ) { 		var table = document.getElementById( TableName ); 		var row = table.insertRow(-1); 		var hU8 = row.insertCell(0);   		var Xd8 = row.insertCell(1);	 	}    	 	static tf5( Sj8, HB2 ) { 		return Sj8.replace(")", ", "+HB2+")").replace("rgb","rgba"); 	}    	 	static bt7( Name ) { 		var result = Name; 		if( Name.localeCompare("BoneSpecies") == 0 ) { 			result = "Preliminary Bone Species" 		} else if( Name.localeCompare("RelationBelongsToUUID") == 0 ) { 			result = "Locus"; 		} else if( Name.localeCompare("RelationIncludesUUID") == 0 ) { 			result = "Includes"; 		} else if( Name.localeCompare("RelationIsAboveUUID") == 0 ) { 			result = "Is Above of"; 		} else if( Name.localeCompare("RelationIsBelowUUID") == 0 ) { 			result = "Is Below of"; 		} else if( Name.localeCompare("CoverageArea") == 0 ) { 			result = "Area (m<sup>2</sup>)"; 		} else if( Name.localeCompare("Square") == 0 ) { 			result = "Square/Space"; 		} else if( Name.localeCompare("Diameter") == 0 ) { 			result = "Diameter (mm)"; 		} else if( Name.localeCompare("CoinWeight") == 0 ) { 			result = "Weight (g.)"; 		} else if( Name.localeCompare("Axis") == 0 ) { 			result = "Axis (hours)"; 		} else if( Name.localeCompare("IssueAuthority") == 0 ) { 			result = "Issuing Authority"; 		} else if( Name.localeCompare("CoinInscription") == 0 ) { 			result = "Inscription obv/rev"; 		} 		return result; 	}   		 	static Ph3( Alias ) { 		var result = Alias; 		return result; 	} 	 	 	 	 	 	 	static getFieldDescription( FieldName ) { 		var result = FieldName; 		if( FieldName.localeCompare("Identifier") == 0 ) { 			result = "The unique inventory number for an object. During item creation, the application proposes the next number once the letter that corresponds to the type of find is entered: A=architecture, B=bronze, C=coin, G=glass, I=inscription, IL=iron and lead, J=jewelry, L=lamp, O=organic, P=pottery, ST=stone, T=terracotta. There is no space between the letter and the number." 		} else if( FieldName.localeCompare("Title") == 0 ) { 			result = "The brief name or description of the object, e.g., Thasian amphora stamp."; 		} else if( FieldName.localeCompare("RelationBelongsToUUID") == 0 ) { 			result = "The locus in which the object was found. During item creation, the locus can be selected from the pull-down menu."; 		} else if( FieldName.localeCompare("Category") == 0 ) { 			result = "This field describes an item more specifically than its Type."; 		} else if( FieldName.localeCompare("Subcategory") == 0 ) { 			result = "Subcategory can aid you in organizing your information. For example, you might add \"Attic\" or \"PRS\" for pottery."; 		} else if( FieldName.localeCompare("ArtifactDate") == 0 ) { 			result = "The date you give the object, as it would appear in a catalogue entry."; 		} else if( FieldName.localeCompare("Dimensions") == 0 ) { 			result = "in meters"; 		} else if( FieldName.localeCompare("Description") == 0 ) { 			result = "Your description of the object. Include the state of preservation."; 		} else if( FieldName.localeCompare("Comparanda") == 0 ) { 			result = "Cite comparanda as needed."; 		} else if( FieldName.localeCompare("Additional bibliography") == 0 ) { 			result = "Provide any additional bibliography as needed. Reference Links menu may help."; 		} else if( FieldName.localeCompare("Notes") == 0 ) { 			result = "This section will not be public but is just for your use."; 		} else if( FieldName.localeCompare("CoinInscription") == 0 ) { 			result = "Enter the inscription on the the coin's obverse and reverse sides separated by a slash."; 		} else if( FieldName.localeCompare("CoverageEarliest") == 0 ) { 			result = "insert one number, adding \"-\" for BC. -300 = 300 BC. 300 = AD 300"; 		} else if( FieldName.localeCompare("CoverageLatest") == 0 ) { 			result = "insert one number, adding \"-\" for BC. -300 = 300 BC. 300 = AD 300"; 		} 		return result; 	}  	 	 	 	 	static Yv8() { 		var result = false; 		if(uv7 == null) { 			try	{ 				var ua = navigator.userAgent.toLowerCase(); 				if ( ua.includes("android") || ua.includes("iphone") ) { 					result = true; 				} 			} catch( ex ) {} 		} else { 			result = uv7; 		} 		return result; 	} 	 	 	 	 		 	static CZ7( s ) { 		if( s == null ) return false; 		var result = true; 		if( s.length == 0 ) result = false; 		for( let i=0; i<s.length; i++ ) { 			var c = s.charAt(i); 			if( c!='0' && c!='1' && c!='2' && c!='3' && c!='4' && c!='5' && c!='6' && c!='7' && c!='8' && c!='9' && c!='-') { 				result = false; 				break; 			} 		}	 		return result; 	} 	 		 	static ContainsFloat( s ) { 		if( s == null ) return false; 		var result = true; 		if( s.length == 0 ) result = false; 		for( let i=0; i<s.length; i++ ) { 			var c = s.charAt(i); 			if( c!='0' && c!='1' && c!='2' && c!='3' && c!='4' && c!='5' && c!='6' && c!='7' && c!='8' && c!='9' && c!='-' && c!='.') { 				result = false; 				break; 			} 		}	 		return result; 	} 	 	 	 	 	 	 	 	 	 	 	 	static HT3( table_Zr2, column_number_to_sort_by, sorting_direction) { 		var table, rows, switching, i, x, y, shouldSwitch, switchcount = 0; 		table = document.getElementById( table_Zr2 ); 		switching = true; 		 		sorting_direction = "asc";  		 		while (switching) { 			 			switching = false; 			rows = table.rows; 			 			for (i = 1; i < (rows.length - 1); i++) { 			   			  shouldSwitch = false; 			   			  x = rows[i].getElementsByTagName("TD")[column_number_to_sort_by]; 			  y = rows[i + 1].getElementsByTagName("TD")[column_number_to_sort_by]; 			   			  if (sorting_direction == "asc") { 				if (x.innerHTML.toLowerCase() > y.innerHTML.toLowerCase()) { 				   				  shouldSwitch= true; 				  break; 				} 			  } else if (sorting_direction == "desc") { 				if (x.innerHTML.toLowerCase() < y.innerHTML.toLowerCase()) { 				   				  shouldSwitch = true; 				  break; 				} 			  } 			} 			if (shouldSwitch) { 			   			  rows[i].parentNode.insertBefore(rows[i + 1], rows[i]); 			  switching = true; 			   			  switchcount ++;       			} else { 				 				if (switchcount == 0 && sorting_direction == "asc") { 					sorting_direction = "desc"; 					switching = true; 				} 			} 		} 	} 	 	 	 	 	 	static removeElementsFromArrayByValue( theArray, theValue ) { 		var result = theArray.concat(); 		var index = result.indexOf( theValue ); 		while( index >= 0 ) { 			theArray.splice(index, 1); 			index = result.indexOf( theValue ); 		} 		return result; 	} 	 	 	static RemoveDuplicatesFromArray( theArray ) { 		var result = theArray.concat(); 		for(var i=0; i<result.length; ++i) { 			for(var j=i+1; j<result.length; ++j) { 				if(result[i] === result[j]) 					result.splice(j--, 1); 			} 		} 		return result; 	} 	 	 	 }	 